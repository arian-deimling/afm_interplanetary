AD - update DB username/password info -> update README.md
AD - remove all secrets from code committed to github (.env file?)
AD - make <select> tags display current value in gray until a valid one is selected
Any - change favicon to Moon
Any - update about page text
Any - update Interface Document w/ events that prompt the website to do something
Any - remove all client-side console.log()
Any - add 'use strict'; to all js code

LINE_COUNT:
cat <(find private public -type f -not -name '*.jpg' -not -name '*.ico' -not -path '*/lib/*' -not -path '*/resources/*')  <(echo -e '.gitignore\nREADME.md\nserver.js\nTODO') | xargs wc 

Production:
AD - enable minify

Bug:
curl -H 'Content-Type: application/json' -d '{"id": 3}' "http://localhost:8080/api/reservation/delete"
